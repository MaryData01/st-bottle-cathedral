services:
  - type: web
    name: stbottle-cathedral
    env: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput
    startCommand: gunicorn stbottle.wsgi